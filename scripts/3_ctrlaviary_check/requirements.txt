# Everything CtrlAviary needs at runtime. gym-pybullet-drones itself is
# installed separately - see requirements-gpd.txt for why.
#
# Verified on Windows with CPython 3.11. Versions match scripts/hil_bridge
# so this sample exercises the same stack the real bridge will use.

# --- direct imports in plant_demo.py ---
numpy==2.4.6
scipy==1.17.1

# pad_demo.py reads the gamepad through XInput via ctypes - part of Windows,
# so there is nothing to install for it. pygame was tried and read only zeros.

# --- pulled in by gym_pybullet_drones.envs.CtrlAviary ---
pybullet==3.2.7
gymnasium==1.3.0
matplotlib==3.11.2
pillow==12.3.0
transforms3d==0.4.2
