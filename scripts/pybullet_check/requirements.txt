# Minimal set to prove PyBullet (and its OpenGL window) works on this machine.
#
# pybullet publishes NO Windows wheels (manylinux only), so pip builds it
# from the sdist - that needs MSVC build tools and takes a few minutes.
# numpy installs from a wheel.
pybullet==3.2.7
numpy==2.2.6
