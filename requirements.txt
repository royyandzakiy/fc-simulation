# One environment for every script under simulator/. Each folder used to carry
# its own .venv, which meant installing pybullet four times over.
#
# gym-pybullet-drones is NOT here - it is not on PyPI. See requirements-gpd.txt.
#
# Verified on Windows with CPython 3.11. Everything installs from a wheel, so
# no MSVC toolchain is needed.

# --- imported directly by the simulator scripts ---
numpy==2.4.6
scipy==1.17.1

# --- the plant: gym_pybullet_drones.envs.CtrlAviary pulls these in ---
pybullet==3.2.7
gymnasium==1.3.0
matplotlib==3.11.2
pillow==12.3.0
transforms3d==0.4.2

# --- hil_bridge only: binary framing and telemetry ---
construct==2.10.70
crc==8.0.0
rerun-sdk==0.37.2          # optional; bridge.py runs without it

# --- transitive, pinned for reproducibility ---
attrs==26.1.0
cloudpickle==3.1.2
contourpy==1.3.3
cycler==0.12.1
Farama-Notifications==0.0.6
fonttools==4.65.0
kiwisolver==1.5.1
packaging==26.3
psutil==7.2.2
pyarrow==25.0.1
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
typing_extensions==4.16.0
