# Everything plant.py needs. fc_sitl.py itself needs only numpy - it has no
# physics, and it reads the gamepad through XInput via ctypes, which is part
# of Windows. gym-pybullet-drones is installed separately, see
# requirements-gpd.txt for why.
#
# Verified on Windows with CPython 3.11. Versions match scripts/hil_bridge
# so this sample exercises the same stack the real bridge will use.

# --- direct imports in plant.py ---
numpy==2.4.6
scipy==1.17.1

# --- pulled in by gym_pybullet_drones.envs.CtrlAviary ---
pybullet==3.2.7
gymnasium==1.3.0
matplotlib==3.11.2
pillow==12.3.0
transforms3d==0.4.2
