# Direct dependencies of bridge.py, plus everything CtrlAviary needs at
# runtime - gym-pybullet-drones itself is installed separately, see
# requirements-gpd.txt for why.
#
# Versions are what was verified working on Windows with CPython 3.11.
#
# Note: pybullet publishes NO Windows wheels (manylinux only), so pip builds
# it from the sdist. That needs MSVC build tools and takes a few minutes.
# Everything else installs from a wheel.

# --- direct imports in bridge.py ---
construct==2.10.70
crc==8.0.0
numpy==2.4.6
scipy==1.17.1
rerun-sdk==0.37.2          # optional; bridge.py runs without it

# --- pulled in by gym_pybullet_drones.envs.CtrlAviary ---
pybullet==3.2.7
gymnasium==1.3.0
matplotlib==3.11.2
pillow==12.3.0
transforms3d==0.4.2

# --- transitive, pinned for reproducibility ---
attrs==26.1.0
cloudpickle==3.1.2
contourpy==1.3.3
cycler==0.12.1
Farama-Notifications==0.0.6
fonttools==4.65.0
kiwisolver==1.5.1
packaging==26.3
psutil==7.2.2
pyarrow==25.0.1
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
typing_extensions==4.16.0
